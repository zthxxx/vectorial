export interface Vector {
  x: number;
  y: number;
}
export interface Rect {
  x: number;
  y: number;
  width: number;
  height: number;
}

export type Matrix = [
  number, number, number,
  number, number, number,
  number, number, number,
]
